"""
Accounts models — top-level shim.

Python's import system resolves `apps.accounts.models` to the
`models/` package directory, not this file.  This file is kept for
editor compatibility only; all model definitions live in
`apps/accounts/models/__init__.py`.
"""
# This file is intentionally shadowed by the models/ package.
# See apps/accounts/models/__init__.py for the actual model definitions.
